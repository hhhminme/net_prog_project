클라이언트, 메인 쓰레드, 서버 안에 있는 계산을 위한 DB 쓰레드로 구성

1. 소켓 설정을 한다.
2. 클라이언트로부터 소켓 모듈이 루프를 돌면서 연결요청을 받고 쓰레드를 생성생성하고 
3. 승인한다. 이후 소켓 정보가 DB로 전달되고, 클라이언트는 쓰레드와 통신을 하게되므로 기존에 서버에 열려있던 소켓을 닫아준다. 
4. 그후 클라이언트로부터 빙고 게임을 진행하기 위한 입력을 받는다. 
5. 전달받은 정보를 통해 게임 오버가 되지 않았다면 루프를 돌면서 만약 입력 정보가 올바르다면 이를 반영 하고 
6. 빙고가 채워졌거나, 예정된 게임 횟수가 지났다면 게임오버의 값을 체크해주고 화면에 출력을 해준다.
7. 게임이 끝나면 각 클라이언트 화면에 결과를 전송하고 순서대로 클라이언트, 서버 안에 있는 쓰레드, 서버가 종료된다.
